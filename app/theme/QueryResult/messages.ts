/*
 * QueryResult Messages
 *
 * This contains all the text for the QueryResult component.
 */
import { defineMessages } from 'react-intl';
const scope = 'app.theme.QueryResult';

export default defineMessages({
  noDataFoundLabel: {
    id: `${scope}.noDataFoundLabel`,
    defaultMessage: 'No Data Found',
  },
});
